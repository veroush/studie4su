---
title: Setup Instructions
description: Local development environment setup for Studie4SU.
---

## Running the Documentation Site (Local)

The documentation site is built using Astro (Starlight). To view or edit these documents locally, follow these steps:

1. Clone the repository and navigate to the `docs` directory:
   \`\`\`bash
   cd docs
   \`\`\`
2. Install the necessary dependencies:
   \`\`\`bash
   npm install
   \`\`\`
3. Start the local development server:
   \`\`\`bash
   npm run dev
   \`\`\`
4. Open your browser and navigate to `http://localhost:4321`.

## Running the Main Web Application
*(Note: Complete setup instructions for the Hono backend and TanStack frontend will be finalized once the migration from Express is complete and the Prisma database is seeded.)*
