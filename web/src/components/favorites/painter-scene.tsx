// Decorative illustration for the Favorites page — optional per the
// component spec, purely visual.
export function PainterScene() {
  return (
    <div aria-hidden="true">
      <img src="/img/painter-1.svg" alt="" />
    </div>
  )
}
